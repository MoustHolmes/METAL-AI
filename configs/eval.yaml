# @package _global_

defaults:
  - _self_
  - data: dict_dataset #dict_dataset #mnist
  - model: transformer_encoder_model #transformer_encoder_model #mnist
  - callbacks: default
  - logger: null #wandb # null # set logger here or use command line (e.g. `python train.py logger=tensorboard`)
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default
  # - override /model: transformer_encoder_model #transformer_encoder_model #mnist
  


task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
